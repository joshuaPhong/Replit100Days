# import the tkinter module
import tkinter as tk
# create the window (gui)
window = tk.Tk()
# add a title tyo the window
window.title("Joshua's first window")
# size the window
window.geometry("400x400")

# run the tkinter window
tk.mainloop()